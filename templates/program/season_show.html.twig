{% extends 'base.html.twig' %}


{% block title %} Saisons {% endblock %}


{% block body %}

    <div class="media">
        <div class="media-body">

            Season {{ season.number }}

            <ul>
                {% for episode in episodes %}
                    <li>
                        {{ episode.number }}


                        <a href="{{ path("program_episode_show", { 'programId': program.id, 'seasonId' : season.id, 'episodeId' : episode.id}) }}"> {{ episode.title }}</a>
                    </li>


                {% endfor %}
            </ul>

        </div>
    </div>

    <a href="{{ path('program_index') }}">
        Retour à la liste des programmes
    </a>

{% endblock %}